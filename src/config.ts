// Configuration constants for the Vectra MCP Server

// Load API URL from environment variable or use default
export const VECTRA_API_URL = process.env.VECTRA_API_URL || 'http://localhost:3000/api';

// Add other configurations here if needed in the future
